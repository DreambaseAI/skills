name: Check hosted release dependencies

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  hosted-dependencies:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node scripts/check-release-urls.mjs
