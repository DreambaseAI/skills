# Override the Dreambase app base URL (defaults to the production app).
# Useful for pointing the installer at a local server during development.
DREAMBASE_URL=http://localhost:3000

# Force the 0600 file-based credential store instead of the OS keychain.
# DREAMBASE_MCP_STORE=file
